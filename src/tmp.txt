


--     mark (x:xs) = Map.foldrWithKey shouldMark (Just Queue) ms *> mark xs  -- asterics> mark xs 
--       where
--       shouldMark k a b = if check x k then notify ms x k else Just Queue
--
--
-- -- | Map, the drawn value, the key
-- notify :: Map Integer (Board BoardNumber) -> Int -> Int -> Maybe Game
-- notify ms x k = do 
--   board <- Map.lookup k ms
--   checking <$> board
--     where
--       checking :: BoardNumber -> BoardNumber
--       checking bn = 
--         if number bn == x
--         then do
--           Bn x True
--           checkWin
--         else
--           bn
--
-- checkWin :: Map Integer (Board BoardNumber) -> ()